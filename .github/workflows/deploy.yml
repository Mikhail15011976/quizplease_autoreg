name: 🚀 Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Для ручного запуска из GitHub UI

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔐 Setup SSH
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}
        SERVER_HOST: ${{ secrets.SERVER_HOST }}
      run: |
        mkdir -p ~/.ssh
        echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H $SERVER_HOST >> ~/.ssh/known_hosts
        echo "✅ SSH настроен"
    
    - name: 🚀 Deploy to Server
      env:
        SERVER_HOST: ${{ secrets.SERVER_HOST }}
        SERVER_USER: ${{ secrets.SERVER_USER }}
        SERVER_DIR: ${{ secrets.SERVER_DEPLOY_DIR }}
      run: |
        echo "🚀 Начинаем деплой на $SERVER_HOST..."
        
        ssh $SERVER_USER@$SERVER_HOST "
          set -e
          echo '=== НАЧАЛО ДЕПЛОЯ ==='
          
          echo '1. 📂 Переходим в директорию проекта...'
          cd '$SERVER_DIR'
          
          echo '2. 🔄 Получаем последние изменения...'
          git fetch origin
          git checkout main
          git reset --hard origin/main
          
          echo '3. 🐍 Активируем виртуальное окружение...'
          source venv/bin/activate
          
          echo '4. 📦 Обновляем зависимости...'
          pip install -r requirements.txt
          
          echo '5. ⚙️ Обновляем systemd...'
          sudo systemctl daemon-reload
          
          echo '6. 🔄 Перезапускаем сервис...'
          sudo systemctl restart quizplease-autoreg
          
          echo '7. ✅ Проверяем статус...'
          sudo systemctl status quizplease-autoreg --no-pager | head -5
          
          echo '=== ДЕПЛОЙ УСПЕШНО ЗАВЕРШЕН ==='
        "
